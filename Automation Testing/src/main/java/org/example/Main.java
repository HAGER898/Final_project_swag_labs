package org.example;

public class Main {
    public static int add(double num1,double num2){
        return (int) (num1+num2);
    }
    public static void main(String[] args) {
        //user login
       /* WebDriver myBrowser = new ChromeDriver();
        WebElement Searchbox;


        /*        String url= "https://www.saucedemo.com/";
        myBrowser.get(url);
        //Maximize the Browser
        myBrowser.manage().window().maximize();
        //standard user login
        myBrowser.findElement(By.id("user-name")).sendKeys("standard_user");
        myBrowser.findElement(By.cssSelector("input[id='password']")).sendKeys("secret_sauce");
        myBrowser.findElement(By.id("login-button")).click();
        //popup message remove
        ChromeOptions options = new ChromeOptions();
        options.addArguments("--disable-save-password-bubble");
        options.addArguments("--disable-features=PasswordManagerOnboarding,PasswordManager,PasswordLeakDetection");
        myBrowser.findElement(By.cssSelector("span[class=\"title\"]"));
        //FluentWait
        FluentWait fluentWait = new FluentWait(myBrowser);
        fluentWait.withTimeout(Duration.ofSeconds(10));
        fluentWait.pollingEvery(Duration.ofSeconds(1));
        fluentWait.until(ExpectedConditions.visibilityOf(myBrowser.findElement(By.xpath("//span[@class='title']"))));



        String currentUrl = myBrowser.getCurrentUrl();
        if (currentUrl.contains("inventory.html")) {
            System.out.println("✅ Login Test Passed");
        } else {
            System.out.println("❌ Login Test Failed. Current URL: " + currentUrl);
        }
        myBrowser.close()
                */







    }
    }
